<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">
    <title>Oficinas</title>
</head>
<body>
    <nav class="navbar navbar-dark bg-dark">
        <h1><a href="/" class="navbar-brand">Colegio Cambridge</a></h1>
    </nav>

    <div class="container">
        <div class="row mt-5">
            <div class="card">
                <div class="card-body">
                    <form action="/oficina/add" method="post">
                        <div class="form-group">
                            <input type="text" name="nombreOficina" placeholder="Nombre Oficina" class="form-control">
                        </div>
                        <div class="form-group">
                            <select name="areaOficina">
                                <option value="0">Area</option>
                                <%for (var i = 0; i < area.length; i++) {%>
                                    <option value="<%= area[i].nombreArea %>"><%= area[i].nombreArea %></option>
                                <% } %>
                            </select>
                        </div>
                        <button type="submit" class="btn btn-primary">Agregar Oficina</button>
                    </form>
                </div>
            </div>
        </div>
        <table class="table table-bordered table-hover mt-5">
            <thead style="text-align: center;">
                <th>ID OFICINA</th>
                <th>NOMBRE</th>
                <th>AREA</th>
                <th>ACCIONES</th>
            </thead>
            <tbody>
                <% if (oficina)  { %>
                    <%for (var i = 0; i < oficina.length; i++) {%>
                       <tr>
                           <td><%= oficina[i].idOficina %></td>
                           <td><%= oficina[i].nombreOficina %></td>
                           <td><%= oficina[i].areaOficina %></td>
                           <td>
                                <a href="/oficina/delete/<%= oficina[i].idOficina %>" class="btn btn-danger">Eliminar</a>
                                <a href="/oficina/update/<%= oficina[i].idOficina %>" class="btn btn-info">Editar</a>
                            </td>
                       </tr>     
                    <% } %>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>
    