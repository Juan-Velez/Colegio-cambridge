<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">

    <script src="https://cdnjs.cloudflare.com/ajax/libs/html2pdf.js/0.10.1/html2pdf.bundle.min.js"></script>

    <title>Informe areas</title>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Colegio Cambridge</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/area">Areas</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/oficina">Oficinas</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/iempleados">Informe Empleados</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/iareas">Informe Areas</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container">
        <h1 style="margin-top: 5rem; text-align: center;">Informe de las areas registradas</h1>
        <% var numeroAreas = 0%>
        <% if (data)  { %>
            <%for (var i = 0; i < data.length; i++) {%> 
                <% var numeroAreas = numeroAreas +1%> 
            <% } %>
        <% } %>
        <br>
        <h3>Cantidad de areas registradas: <%= numeroAreas%></h3>
        <table class="table table-bordered table-hover mt-5" id="tabla">
            <thead style="text-align: center;">
                <br>
                <th>ID</th>
                <th>NOMBRE</th>
            </thead>
            <tbody style="text-align: center;">
                <% if (data)  { %>
                    <%for (var i = 0; i < data.length; i++) {%>
                        <tr>
                            <td><%= i+1 %></td>
                            <td><%= data[i].nombreArea%></td>
                        </tr>   
                        <% var numeroAreas = numeroAreas +1%> 
                    <% } %>
                <% } %>
            </tbody>
        </table>
    </div>
    <script src="script.js"></script>
</body>
</html>