<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">
    <title>Colegio</title>
</head>
<body>
    <nav class="navbar navbar-dark bg-dark">
        <h1><a href="/" class="navbar-brand">Colegio Cambridge</a></h1>
        <h1><a href="/area" style="color: beige;">Areas</a></h1>
    </nav>
    <div class="container">
        <div class="row mt-5">
            <div class="card">
                <div class="card-body">
                    <form action="/add" method="post">
                        <div class="form-group">
                            <input type="text" name="nombre" placeholder="Nombre" class="form-control">
                        </div>
                        <div class="form-group">
                            <select name="area">
                                <option value="0">Escoga el areaa la cual fua asigado</option>
                                <%for (var i = 0; i < resultado2.length; i++) {%>
                                    <option value="<%= resultado2[i].nombreArea %>"><%= resultado2[i].nombreArea %></option>
                                <% } %>
                            </select>
                        </div>
                        <div class="form-group">
                            <input type="text" name="tipo_empleado" placeholder="tipo de empleado" class="form-control">
                        </div>

                        <div class="form-group">
                            <input type="text" name="oficina" placeholder="oficina" class="form-control">
                        </div>
                        <div class="form-group">
                            <input type="text" name="tipo_contrato"  placeholder="tipo de contrato" class="form-control">
                        </div>
                        <button type="submit" class="btn btn-primary">Agregar empleado</button>
                    </form>

                </div>
            </div>
        </div>
        <table class="table table-bordered table-hover mt-5">
            <thead style="text-align: center;">
                <th>ID</th>
                <th>NOMBRE</th>
                <th>AREA</th>
                <th>TIPO DE EMPLEADO</th>
                <th>OFICINA</th>
                <th>TIPO DE CONTRATO</th>
                <th>ACCIONES</th>
            </thead>
            <tbody>
                <% if (resultado)  { %>
                    <%for (var i = 0; i < resultado.length; i++) {%>
                       <tr>
                           <td><%= resultado[i].id %></td>
                           <td><%= resultado[i].nombre%></td>
                           <td><%= resultado[i].area%></td>
                           <td><%= resultado[i].tipo_empleado%></td>
                           <td><%= resultado[i].oficina%></td>
                           <td><%= resultado[i].tipo_contrato%></td>
                           <td>
                                <a href="/delete/<%= resultado[i].id %>" class="btn btn-danger">Eliminar</a>
                                <a href="/update/<%= resultado[i].id %>" class="btn btn-info">Editar</a>
                            </td>
                       </tr>     
                    <% } %>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>
    